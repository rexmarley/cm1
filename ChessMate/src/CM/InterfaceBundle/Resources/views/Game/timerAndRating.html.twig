<div id="timer{{ timer }}" class="row timer">
	<div class="col-md-12">
		<h1><span id="pName{{ timer }}">{{ user.username }} </span><label id="rating{{ timer }}">({{ user.rating }})</label>: &nbsp;<span id="tLeft{{ timer }}">{{ timeLeft }}</span></h1>
	</div>
</div>